/*

package view;

import java.awt.*;

public class BasePanel {
    public Panel basePanel;
    public Panel logButtonPanel;
    public Panel inputPanel;
    public Panel buttonPanel;
    public Panel logPanel;

    public BasePanel(Panel logButtonPanel, Panel inputPanel, Panel buttonPanel,Panel logPanel){
        basePanel = new Panel(new BorderLayout());

        this.logButtonPanel = logButtonPanel;
        this.inputPanel = inputPanel;
        this.buttonPanel = buttonPanel;
        this.logPanel = logPanel;

        basePanel.add(logButtonPanel);
        basePanel.add(inputPanel);
        basePanel.add(buttonPanel);
        basePanel.add(logPanel);
    }



}

*/
